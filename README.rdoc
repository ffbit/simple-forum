== A Simple Forum

It is just for playing around with Ruby on Rails.

=== It consists of
* users (regular and administrative);
* categories;
* forums;
* topics;
* posts.

=== Dependencies

    sudo apt-get install imagemagick
