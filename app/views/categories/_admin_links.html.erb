<% if user_admin? %>
  <div>
    <%= link_to 'Edit', edit_category_path(category) %>
  </div>
  <div>
    <%= link_to 'Delete', category, confirm: 'Are you sure?', method: :delete %>
  </div>
<% end %>
